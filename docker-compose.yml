version: 'version'

services:
  web:
    build: .
    ports:

  db:
    image: "mysql"
    environment:
      MYSQL_ROOT_PASSWORD: MYSQL_ROOT_PASSWORD
    volumes:
      - db-data:/foo

volumes:
 db-data: